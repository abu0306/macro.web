server {
  listen 4010;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA served under /tauri/
  location ^~ /tauri/ {
    try_files $uri $uri/ /index.html;
  }

  # Redirect root to /tauri/
  location = / {
    return 302 /tauri/;
  }
}


