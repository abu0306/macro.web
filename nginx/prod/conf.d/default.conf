# 生产环境反向代理配置

# Next.js App 1
server {
    listen 80;
    server_name nextjs-app1.local;

    location / {
        proxy_pass http://nextjs-app1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 生产环境缓存
        proxy_cache STATIC;
        proxy_cache_valid 200 1d;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_lock on;
    }

    # 静态资源缓存
    location /_next/static/ {
        proxy_pass http://nextjs-app1:3001;
        proxy_cache STATIC;
        proxy_cache_valid 200 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Next.js App 2
server {
    listen 80;
    server_name nextjs-app2.local;

    location / {
        proxy_pass http://nextjs-app2:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 生产环境缓存
        proxy_cache STATIC;
        proxy_cache_valid 200 1d;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_lock on;
    }

    # 静态资源缓存
    location /_next/static/ {
        proxy_pass http://nextjs-app2:3002;
        proxy_cache STATIC;
        proxy_cache_valid 200 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Vite App 1
server {
    listen 80;
    server_name vite-app1.local;

    location / {
        proxy_pass http://vite-app1:4001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 生产环境缓存
        proxy_cache STATIC;
        proxy_cache_valid 200 1d;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_lock on;
    }

    # 静态资源缓存
    location /assets/ {
        proxy_pass http://vite-app1:4001;
        proxy_cache STATIC;
        proxy_cache_valid 200 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Vite App 2
server {
    listen 80;
    server_name vite-app2.local;

    location / {
        proxy_pass http://vite-app2:4002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 生产环境缓存
        proxy_cache STATIC;
        proxy_cache_valid 200 1d;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_lock on;
    }

    # 静态资源缓存
    location /assets/ {
        proxy_pass http://vite-app2:4002;
        proxy_cache STATIC;
        proxy_cache_valid 200 30d;
        add_header Cache-Control "public, immutable";
    }
}

# 默认路由 - 重定向到Next.js App 1
server {
    listen 80 default_server;
    server_name _;

    location / {
        return 301 http://nextjs-app1.local;
    }
}
